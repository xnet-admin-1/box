# rootfs

Ubuntu 24.04 base rootfs repackaged for Box/proot.

- Source: Canonical ubuntu-base cloud images
- Format: tar.xz with `--hard-dereference` (Android hardlink workaround)
- Architecture: aarch64
